# pip install -r requirements.txt

# ------------------------------------------
# 🌐 Django и сопутствующие пакеты
# ------------------------------------------
Django>=5.2,<6.0         # Веб-фреймворк, основа всего проекта

# ------------------------------------------
# 📊 Обработка данных
# ------------------------------------------
pandas>=2.0              # Построение DataFrame из EntryValue и чтение Excel
numpy>=1.24              # Поддержка числовых операций, требуется pandas

# ------------------------------------------
# 🤖 Машинное обучение (линейная модель)
# ------------------------------------------
scikit-learn>=1.2        # Используется для LinearRegression (базовая модель)
joblib>=1.2              # Сохраняем и загружаем модели в формате .pkl

# ------------------------------------------
# 📥 Загрузка Excel-файлов через админку
# ------------------------------------------
openpyxl>=3.1            # Движок для чтения .xlsx в pandas.read_excel()

# ------------------------------------------
# 🛠️ Разное (опционально, для удобства)
# ------------------------------------------
ipython                  # Удобный интерактивный shell (manage.py shell_plus)
python-dotenv            # Для подключения .env-файлов, если используешь их
